# simple questionaire testing example by `@xstate/test`.

This example creates two test methods:

- `yarn test`: just test state machine `questionaireMachine`.

- `yarn dev` & `yarn cy:open`: e2e test localhost:8080 page.
